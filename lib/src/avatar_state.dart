/// The conversational state represented by the avatar's motion.
enum FlowAvatarState { idle, listening, thinking, speaking, success, error }

/// The clipping shape of a FlowAvatar.
enum FlowAvatarShape { circle, roundedSquare, square }
